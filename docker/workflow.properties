#
# Configuration file for the workflow engine (Flowable).
#
# No SMTP configuration is needed here: all emails are sent by the
# KAWA server and its SMTP configuration (see configuration/kawa.config).
#
# The security key (app.auth.clients.kawa) is provided through the
# APP_AUTH_CLIENTS_KAWA environment variable, generated by the installer.
#
